// Minimal reference ("stub") for SDRSharp.PanView. The plugin does not use
// PanView types directly, but SDRSharp.Common.ISharpControl references this
// assembly, so it must be present for compilation. See ../README.md.

namespace SDRSharp.PanView
{
    public enum BandType { Lower, Upper, Center }
}
